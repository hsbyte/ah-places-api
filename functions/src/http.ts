enum HTTP {
    // Successful responses
    OK = 200,
    Created = 201,
    // Client error responses
    Unauthorized = 401,
    // Server error responses
    InternalServerError = 500,
}

export default HTTP